export const HttpServerConfig = {
    HOST: process.env.SERVER_HOST || 'localhost',
    PORT: process.env.SERVER_PORT || '8999',
};

export const TcpServerConfig = {
    HOST: process.env.SERVER_HOST || 'localhost',
    PORT: process.env.SERVER_PORT || '8888',
}
